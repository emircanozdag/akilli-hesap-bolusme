{
  "name": "akilli-hesap-bolusme",
  "version": "0.1.0",
  "private": true,
  "description": "SplitTab — AI fiş tarama + mobil hesap bölüşme uygulaması (monorepo)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
