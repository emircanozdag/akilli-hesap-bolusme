{
  "name": "@ahb/site",
  "version": "0.1.0",
  "private": true,
  "description": "SplitTab tanıtım sitesi — mobil uygulama landing + gizlilik (Cloudflare Pages)",
  "scripts": {
    "preview": "python3 -m http.server 4173",
    "deploy": "wrangler pages deploy . --project-name=akilli-hesap-bolusme"
  }
}
