# Mobil istemci ortam değişkenleri (kopyala → .env).
# Expo bu dosyayı otomatik okur; Git'e EKLENMEZ.

# Production backend (HTTPS). Bu satırı kopyalayıp .env dosyasına yapıştır.
EXPO_PUBLIC_AHB_API=https://ahb-server.emirozdag.workers.dev
